%a.addmore{:href => "/admin/users/new"} add new +
%h1 Klinic staff
%table.datatable{:style => "width: 100%"}
  %thead
    %tr
      %th Name
      %th Email
      %th Mobile
      %th Roles
      %th Scheduled until
      %th.right.nosort
  %tbody    
    - @users.each do |f|
      %tr.selectable{:rel => "/admin/users/#{f.pk}"}
        %td= f.name
        %td
          %a{:href => "mailto:#{f.email}"}= f.email
          
        %td= f.mobile
        %td
          -if f.admin?
            %span.badge admin
          - if f.vet?
            %span.badge vet
          - if f.behaviourist?            
            %span.badge behaviourist
          - if f.assistant?
            %span.badge assistant
          - if f.media
            %span.badge media manager
        %td= f.pattern_apply_until.uk rescue '–'
        %td.right.nosort
          %a{:href => "/admin/users/#{f.id}"}
            %img{:src => '/img/icons/edit.png', :title => "edit #{f.name}"}
          %a.delete_link{:href => "/admin/users/#{f.id}", :rel => "Are you sure you want to delete this user?", :rev => "/admin/users"}
            %img{:src => '/img/icons/delete.png', :title => "delete #{f.name}"}
